// Number
var friendsAge = [25, 23, 26, 28, 27];

// String
var friendsName = ['shamim', 'emon', 'nazmul', 'anik'];


// that's for counting array elements
console.log(friendsName.length);
// can put it in a variable as well
var ageCount = friendsAge.length;
console.log(ageCount);